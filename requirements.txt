# Base dependency constraints (human-edited).
# Use `requirements.lock.txt` for reproducible installs and CI.
setuptools>=68
wheel>=0.42
openpyxl>=3.1.0
pypdf>=4.2.0
Pillow>=10.0.0
pytesseract>=0.3.10
typer>=0.12.0
PyYAML>=6.0.1
python-dotenv>=1.0.1
pytest>=8.2.0
