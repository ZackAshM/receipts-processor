# models

Purpose:
- Stores schema reference files that define output structure.

Contents:
- `model.csv` / `model.xlsx`: canonical target schema.
- `example.csv` / `example.xlsx`: example output guidance.
