# src/receipt_processor/processing

Purpose:
- Deterministic post-extraction processing that computes normalized expense fields.

Contents:
- `expense_processor.py`: computes processed keywords (for example `true_expense`, `receipt_expense`, `receipt_amount_if_different`) and run-level summary operations.
