"""Convert raw receipt text into normalized expense fields."""


def parse_receipt_text(raw_text: str) -> dict:
    """Parse receipt text into a base field dictionary.

    Placeholder implementation for project bootstrap.
    """
    _ = raw_text
    return {}
